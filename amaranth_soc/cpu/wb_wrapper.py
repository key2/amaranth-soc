"""Wishbone CPU wrapper (not yet implemented).

This module will provide a wrapper for CPU cores that use a Wishbone
bus interface for instruction fetch and data access.

Planned features:
- Wishbone master interface for instruction bus (ibus)
- Wishbone master interface for data bus (dbus)
- Interrupt vector input
- Integration with amaranth_soc.wishbone bus infrastructure
"""
